
###> symfony/framework-bundle ###
# APP_SECRET is injected per-environment by Docker Compose from the
# root / staging / production .env files. Kept empty here so no real
# secret is ever committed to the repository.
APP_SECRET=
###< symfony/framework-bundle ###
